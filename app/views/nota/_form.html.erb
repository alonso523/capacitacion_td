<%= form_for(@notum) do |f| %>
  <% if @notum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notum.errors.count, "error") %> prohibited this notum from being saved:</h2>

      <ul>
      <% @notum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grupo_id %><br>
    <%= f.collection_select :grupo_id, Grupo.all, :id, :descripcion %><br />
  </div>
  <div class="field">
    <%= f.label :estudiante_id %><br>
    <%= f.collection_select :estudiante_id, Estudiante.all, :id, :nombre %><br />
  </div>
  <div class="field">
    <%= f.label :nota %><br>
    <%= f.number_field :nota %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit "Crear Nota"%>
  </div>
<% end %>
