<%= form_for(@grupo) do |f| %>
  <% if @grupo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grupo.errors.count, "error") %> prohibited this grupo from being saved:</h2>

      <ul>
      <% @grupo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :curso_id %><br>
    <%= f.collection_select :curso_id, Curso.all, :id, :nombre %><br />
  </div>
  <div class="field">
    <%= f.label :profesor_id %><br>
    <%= f.collection_select :profesor_id, Profesore.all, :id, :nombre %><br />
  </div>
  <div class="field">
    <%= f.label :sede_id %><br>
    <%= f.collection_select :sede_id, Sede.all, :id, :nombre %><br />
  </div>
  <div class="field">
    <%= f.label :asistente_id %><br>
    <%= f.collection_select :asistente_id, Asistente.all, :id, :nombre %><br />
  </div>
  <div class="field">
    <%= f.label :Descripción %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit "Crear Grupo"%>
  </div>
<% end %>
